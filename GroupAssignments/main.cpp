#include "/home/bigtasty/COSC2436/ArgumentManager.h"
#include <algorithm>
#include <iostream>
#include <fstream>
#include <string>
#include <vector>
#include "Node.h"


string linkedList::reverse(vector<int> inV, int begin, int end)
{
    //remove
    int isClosed = 0;


    for(int i = 0; i < inV.size(); i++)
    {
        //check if there
    }
}


void reverseLL(Node* head, std::string inStr)
{
    Node* tempHead = new Node;
    Node* cu = head;
    if(inStr.find("(") == string::npos && inStr.find(")") == string::npos)
    {
        return;
    }

    else
    {
        
    }
}

int main()
{
    std::vector<std::string> input = {"0(945)23", "12(39)", "(34)323", "(2345)4390"};

    for(int i = 0; i < input.size(); i++)
    {
        if(i == 0)
        {

        }
    }
}